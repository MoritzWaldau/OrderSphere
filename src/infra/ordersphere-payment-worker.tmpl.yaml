api-version: 2024-02-02-preview
location: {{ .Env.AZURE_LOCATION }}
identity:
  type: UserAssigned
  userAssignedIdentities:
    ? "{{ .Env.AZURE_CONTAINER_REGISTRY_MANAGED_IDENTITY_ID }}"
    : {}
properties:
  environmentId: {{ .Env.AZURE_CONTAINER_APPS_ENVIRONMENT_ID }}
  configuration:
    activeRevisionsMode: single
    runtime:
      dotnet:
        autoConfigureDataProtection: true
    registries:
      - server: {{ .Env.AZURE_CONTAINER_REGISTRY_ENDPOINT }}
        identity: {{ .Env.AZURE_CONTAINER_REGISTRY_MANAGED_IDENTITY_ID }}
    secrets:
      - name: connectionstrings--azure-service-bus
        value: '{{ .Env.AZURE_SERVICE_BUS_SERVICEBUSENDPOINT }}'
      - name: connectionstrings--payment-db
        value: '{{ .Env.POSTGRES_CONNECTIONSTRING }};Database=payment-db'
      - name: keycloak--clientsecret
        value: '{{ securedParameter "payment_worker_secret" }}'
  template:
    containers:
      - image: {{ .Image }}
        name: ordersphere-payment-worker
        env:
          - name: AZURE_CLIENT_ID
            value: {{ .Env.MANAGED_IDENTITY_CLIENT_ID }}
          - name: AZURE_SERVICE_BUS_HOST
            value: '{{ .Env.AZURE_SERVICE_BUS_SERVICEBUSHOSTNAME }}'
          - name: AZURE_SERVICE_BUS_URI
            value: '{{ .Env.AZURE_SERVICE_BUS_SERVICEBUSENDPOINT }}'
          - name: Keycloak__Authority
            value: '{{ parameter "keycloak_realm_authority" }}'
          - name: Keycloak__ClientId
            value: payment-worker
          - name: OTEL_DOTNET_EXPERIMENTAL_OTLP_RETRY
            value: in_memory
          - name: PAYMENT_DB_DATABASENAME
            value: payment-db
          - name: PAYMENT_DB_HOST
            value: '{{ .Env.POSTGRES_HOSTNAME }}'
          - name: PAYMENT_DB_JDBCCONNECTIONSTRING
            value: jdbc:postgresql://{{ .Env.POSTGRES_HOSTNAME }}/payment-db?sslmode=require&authenticationPluginClassName=com.azure.identity.extensions.jdbc.postgresql.AzurePostgresqlAuthenticationPlugin
          - name: PAYMENT_DB_PORT
            value: "5432"
          - name: PAYMENT_DB_URI
            value: postgresql://{{ .Env.POSTGRES_HOSTNAME }}/payment-db
          - name: Payment__BypassProviders
            value: '{{ parameter "payment_bypass_providers" }}'
          - name: ConnectionStrings__azure-service-bus
            secretRef: connectionstrings--azure-service-bus
          - name: ConnectionStrings__payment-db
            secretRef: connectionstrings--payment-db
          - name: Keycloak__ClientSecret
            secretRef: keycloak--clientsecret
    scale:
      minReplicas: 1
tags:
  azd-service-name: ordersphere-payment-worker
  aspire-resource-name: ordersphere-payment-worker
